block('menu')(
    def()(function() {
        applyNext({
            _alfaMods : {
                bkg : this.mods.bkg,
                horizontal : this.mods.horizontal
            }
        });

    })
);

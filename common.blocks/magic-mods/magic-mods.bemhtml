block('*')(
    match(function() { return this._mods; }).def()(function() {
        if (this.ctx.mods) {
            this._theme = this.ctx.mods.theme || this._mods.theme;
            this._size = this.ctx.mods.size || this._mods.size;
            this.ctx.mods = this.extend(this.ctx.mods, {
                theme: this._theme,
                size: this._size
            });
        }
        return applyNext();
    })
);

block('form-field')(
    match(function() { return this._mods && this._mods.view; }).def()(function() {
        this.ctx.mods.view = this._mods.view;
        return applyNext({
            _mods: {
                theme: this._mods.theme || this.ctx.mods.theme,
                size: this._mods.size || this.ctx.mods.size
            }
        });
    })
);

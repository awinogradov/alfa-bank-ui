block('form-field')(
    match(function() { return this._mods && this._mods.view; }).def()(function() {
        var mods = this._mods || this.mods;
        this.mods.view = mods.view;
        return applyNext({
            _mods: {
                theme: mods.theme || mods.theme,
                size: mods.size || mods.size
            }
        });
    })
);

block('form-field')(
    js()(true),
    def()(function() {
        var formField = this.ctx;
        formField.id = formField.id || this.generateId();
        formField.mods.name = formField.name;
        applyNext({ formField: formField });
    })
);

block('input').mod('mode', 'result')(
    def()(function() {
        applyNext({ _input: this.ctx });
    }),

    elem('box')(
        content()(function() {
            return [
                {
                    elem: 'inner',
                    mods: {
                        theme: this._input.mods.theme
                    },
                    content: this._input.val
                }
            ];
        })
    )
);

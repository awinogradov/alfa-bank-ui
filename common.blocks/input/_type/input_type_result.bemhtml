block('input').mod('type', 'result')(
    def()(function() {
        applyNext({ _input : this.ctx });
    }),

    elem('box')(
        content()(function() {
            return [
                {
                    elem : 'result',
                    mods : {
                        theme : this._input.mods.theme,
                        bkg : this._input.mods.bkg
                    },
                    content : this._input.val
                }
            ];
        })
    )
);

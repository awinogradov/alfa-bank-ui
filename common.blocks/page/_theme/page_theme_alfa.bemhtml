block('page').mod('theme', 'alfa')(
    def()(function() {
        var ctx = this.ctx;

        ctx.styles = ctx.styles || [];
        ctx.styles.push({
            elem : 'css',
            url : '//fonts.googleapis.com/css?family=Roboto:100,300,400,500,900&subset=latin,cyrillic'
        });

        applyCtx(ctx);
    })
);

block('calendar').mix()(function() {
    var ctx = this.ctx;
    return {
        block : 'popup',
        mods : this.extend(ctx.mods, {
            'has-calendar' : true,
            target : 'anchor',
            bkg : false
        })
    };
});

block('form')(
    def()(function() {
        var mods = this.ctx.mods || {};
        return applyNext({
            _mods: {
                theme: mods.theme,
                size: mods.size,
                view: mods.view
            }
        });
    })
);

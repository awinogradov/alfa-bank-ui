block('mask')(
    tag()('span'),
    js()(true),
    content()(function() {
        if(this.ctx.val){
            return this.ctx.val.replace(/\*/g, '●');
        }
        return this.ctx.content;
    })
);

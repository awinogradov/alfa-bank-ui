block('mask')(
    tag()('span'),
    js()(true),
    content()(function() {
        if(this.ctx.val){
            var num = this.ctx.val.split(/\*+/);
            return num[0] + '●●●●' + num[1];
        }
        return this.ctx.content;
    })
);

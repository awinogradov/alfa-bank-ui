block('header')(

    js()(true),

    content()(function() {
        return [
            {
                elem : 'inner',
                content : applyNext({
                    _headerMods : this.mods,
                    _header : this.ctx
                })
            }
        ];
    })
);

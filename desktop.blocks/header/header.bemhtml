block('header')(

    js()(true),

    content()(function() {
        var _this = this;

        return [
            {
                elem : 'inner',
                content : applyNext({
                    _headerMods : _this.mods,
                    _header : _this.ctx
                })
            }
        ];
    })
);

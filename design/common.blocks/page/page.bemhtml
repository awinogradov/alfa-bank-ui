block('page')(

    def()(function() {
        var ctx = this.ctx;

        ctx.mobify? ctx.head = [
            {
                elem : 'meta',
                attrs : {
                    name : 'viewport',
                    content : 'width=device-width, initial-scale=1.0'
                }
            }
        ] : ctx.head = [];

        applyNext();
    })
)
